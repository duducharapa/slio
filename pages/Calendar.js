import React from 'react';
import { Text, StyleSheet } from 'react-native';

import Background from '../components/Background';

function Calendar() {
  return(
    <Background>
      <Text>cheiro de s</Text>
    </Background>
  );
}

const styles = StyleSheet.create({
  
});

export default Calendar;